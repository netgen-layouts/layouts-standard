{% extends '@NetgenLayouts/app/block/block.html.twig' %}

{% import '@NetgenLayouts/app/parts/macros.html.twig' as macros %}

{% trans_default_domain 'nglayouts_app' %}

{% block content %}
    {% set number_of_columns = block.parameter('number_of_columns').value|default(3) %}

    {% include '@NetgenLayouts/app/parts/collection.html.twig' with {
        block: block,
        collection: collections.default is defined ? collections.default : null,
        number_of_columns: block.parameter('number_of_columns').value|default(3),
    } %}
{% endblock %}
